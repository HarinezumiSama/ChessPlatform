namespace ChessPlatform.UI.Desktop
{
    internal enum SquareMode
    {
        Default,
        ValidMoveSource,
        ValidMoveTarget,
        CurrentMoveSource,
        CurrentMoveTarget
    }
}