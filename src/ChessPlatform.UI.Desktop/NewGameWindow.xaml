<Window x:Class="ChessPlatform.UI.Desktop.NewGameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ChessPlatform.UI.Desktop"
        xmlns:viewModels="clr-namespace:ChessPlatform.UI.Desktop.ViewModels"
        Title="New Game" Width="600" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" SizeToContent="Height" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="_White Player">
            <local:PlayerChoiceControl x:Name="WhitePlayerChoiceControl" x:FieldModifier="private" />
        </GroupBox>

        <GroupBox Grid.Row="1" Header="_Black Player">
            <local:PlayerChoiceControl x:Name="BlackPlayerChoiceControl" x:FieldModifier="private" />
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" MinWidth="80" Margin="10,5,10,5" Click="OKButton_OnClick" IsDefault="True" />
            <Button Content="Cancel" MinWidth="80" Margin="10,5,10,5" Click="CancelButton_OnClick" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>